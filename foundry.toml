[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
evm_version = "paris"

gas_reports = ["METH", "METHDeployer"]

[fuzz]
runs = 256

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
